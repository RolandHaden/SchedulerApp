<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="autoscroll">
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:scrollHorizontally">true</item>
    </style>
    <style name="CalenderViewCustom" parent="Theme.AppCompat">
        <item name="colorAccent">@color/burnt</item>
        <item name="android:textColorPrimary">@color/beige</item>
        <item name="colorPrimary">@color/beige</item>
    </style>

    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/beige</item>
        <item name="android:textSize">16sp</item>
        <item name="android:weekNumberColor">@color/dim_beige</item>
    </style>

    <style name="CalenderViewWeekCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/dim_beige</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="fab_3_rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">25%</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/ltgray</item>
    </style>

    <style name="CustomDatePickerDialog" parent="Theme.AppCompat">
        <item name="android:datePickerStyle">@style/CustomDatePickerStyle</item>
        <item name="android:windowBackground">@drawable/floating_dialog_shape</item>
        <item name="android:colorAccent">@color/burnt</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- You can also add padding or margins if you want to reduce the size of the dialog -->
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <!-- Add other styling attributes if needed -->
    </style>

    <style name="CustomDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <!-- Customize your DatePicker style here -->
        <!-- You can also define custom attributes for the internal views of the DatePicker -->
    </style>

</resources>